services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: vault_db
      POSTGRES_USER: vault
      POSTGRES_PASSWORD: vault
    ports: ['5433:5432']

  redis:
    image: redis:7-alpine
    ports: ['6379:6379']
